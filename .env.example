# cwv-agent optional provider configuration.
# The default `local` profile does not require this file.
#
# Copy to `.env` only when you opt into one of the provider profiles below.
# Keep real values out of git.

# field-google: CrUX + PageSpeed Insights triage.
GOOGLE_CRUX_API_KEY=
GOOGLE_PAGESPEED_INSIGHTS_API_KEY=
